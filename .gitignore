# build output
# dist/ is committed on purpose: Zeabur serves the prebuilt files with
# Caddy (see Dockerfile) so the 2 GB server never runs a Node build.
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*

# environment
.env
.env.production

# os
.DS_Store
Thumbs.db
